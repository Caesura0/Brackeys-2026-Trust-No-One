using UnityEngine;

namespace GameData.Utils
{
    /// <summary>
    /// Attribute used to display a string field as a dropdown of available Case IDs in the Inspector.
    /// </summary>
    public class CaseIdAttribute : PropertyAttribute
    {
    }
}
